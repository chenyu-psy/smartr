% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-helper.R
\name{agg_multinomial}
\alias{agg_multinomial}
\title{Aggregate data using a multinomial distribution}
\usage{
agg_multinomial(
  data,
  responses,
  group = NULL,
  DV_name = NULL,
  nDV_name = "nRet",
  numeric = TRUE
)
}
\arguments{
\item{data}{A data frame containing the data to be aggregated}

\item{responses}{Variables to be treated as responses. Can be provided as:
\itemize{
\item A character vector of column names
\item Unquoted variable names using tidyverse-style selection (e.g., \code{starts_with("resp")})
}}

\item{group}{Variables to group by. Can be provided as:
\itemize{
\item A character vector of column names
\item Unquoted variable names (e.g., \code{c(subject, condition)})
\item NULL for no grouping (default)
}}

\item{DV_name}{The name for the dependent variable column in the output. If NULL (default),
the response variables will not be combined into a matrix.}

\item{nDV_name}{The name for the column containing the total number of responses (default: "nRet")}

\item{numeric}{Logical. If TRUE (default), rename the matrix columns to sequential numbers.
If FALSE, keep original response variable names.}
}
\value{
A data frame containing the aggregated data with:
\itemize{
\item The grouping variables (if any)
\item Either individual response counts or a matrix of responses (depending on DV_name)
\item A column with the total number of responses
}
}
\description{
This function aggregates data for multinomial analysis by counting responses across
specified categories. It supports both standard and tidyverse-style variable input
through tidy evaluation.
}
