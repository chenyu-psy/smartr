% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{mutate_formula}
\alias{mutate_formula}
\title{Add New Columns to a Data Frame Using Formulas}
\usage{
mutate_formula(.data, ...)
}
\arguments{
\item{.data}{A data frame or data frame extension (e.g., a tibble).}

\item{...}{One or more formulas used to create new columns. Each formula should have
the form \code{new_column_name ~ expression}.}
}
\value{
A data frame with new columns added based on the provided formulas.
}
\description{
This function adds new columns to a data frame by evaluating R formulas.
It processes each formula to extract the target column name (left-hand side)
and evaluates the right-hand side expression using the data frame's existing columns.
The interface mimics dplyr's \code{mutate()} function but uses formulas for column creation.
}
\examples{
df <- data.frame(x = 1:5, y = 6:10)
mutate_formula(df, z ~ x + y, w ~ x * y)
mutate_formula(df, ratio ~ x/y, sum_sq ~ x^2 + y^2)

}
